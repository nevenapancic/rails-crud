class Restaurant < ApplicationRecord
  # restaurant model that is inheriting from application record which is abstract and it connects all of it to active record
  #just it means it will have active record available
  # look at migration file in db
end
